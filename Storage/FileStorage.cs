using System.Collections.Generic;
using System.IO;
using UnityEngine;

namespace TouhouMix.Storage {
	public sealed class FileStorage {
//		readonly string JSON_STORAGE_FILE_PATH = Path.Combine(Application.persistentDataPath, "JsonStorage");
	}
}